
@{
    ViewBag.Title = "Login";
}

<br />

<label><B>PROJE İLE İLGİLİ LOG KAYITLARI:</B> http://localhost:2624/elmah.axd </label>
<br />
<script src="~/Scripts/jquery-3.0.0.min.js"></script>
@model ProjeYonetimSistemi_UI.Models.UserPassword
<div class="row">
    <div class="col-lg-12">
        <section class="panel">
            <header class="panel-heading">
                Login
            </header>
            @ViewBag.durum
            <div class="panel-body">
                <form action="/Login/Login" class="form-horizontal tasi-form" method="post">

                    <div class="form-group">

                        <div class="col-sm-10">
                            @*<input type="text" class="form-control" name="KullaniciAdi" placeholder="Kullanıcı adı" id="KullaniciAdi">*@
                            @Html.TextBoxFor(o => o.KullaniciAdi, new { @class = "form-control", @placeholder = "Kullanıcı Adı giriniz" })
                            @Html.ValidationMessageFor(m => m.KullaniciAdi, "", new { @class = "text-danger", id = "KullaniciAdi" })
                        </div>
                    </div>
                    <div class="form-group">

                        <div class="col-sm-10">
                            @*<input type="text" class="form-control" name="Password" placeholder="Sifre" id="Password">*@
                            @Html.PasswordFor(o => o.Password, new { @class = "form-control", @placeholder = "Password " })
                            @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger", id = "Password" })
                        </div>
                    </div>

                    <div class="form-group">
                        <div class="col-lg-offset-2 col-lg-10">
                            <button class="btn btn-danger" id="login" type="submit">Sign In</button>
                        </div>
                    </div>

                </form>
            </div>
        </section>

    </div>
</div>
@*<script>
    $(document).ready(function myfunction() {
        $("#login").click(function () {
            $.ajax({
                url: "/Login/Login",
                type: "POST",
                data: {
                    KullaniciAdi: $("#KullaniciAdi").val(),
                    Password: $("#Password").val()
                },
                success: function (data) {
                    if (data == "E")
                    {
                        
                        window.location.href = '/Proje/Projeler';
                        
                    
                    }
                    else {
                        alert("Login Basarısız");
                    }
                },
                error: function () {
                    alert("Bir Sorun Yaşandı");
                }

            });


        });
    });
</script>*@




